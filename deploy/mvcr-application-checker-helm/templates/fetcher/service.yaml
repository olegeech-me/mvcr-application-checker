{{- if and .Values.fetcher.enabled .Values.metrics.enabled .Values.metrics.fetcher.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvcr-application-checker.fullname" . }}-fetcher-metrics
  labels:
    {{- include "mvcr-application-checker.componentLabels" (dict "context" . "component" "fetcher") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "mvcr-application-checker.selectorLabels" (dict "context" . "component" "fetcher") | nindent 4 }}
{{- end }}
